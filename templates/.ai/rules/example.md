---
description: Example rule shipped by agent-config-sync
alwaysApply: false
---

# Example rule

Replace this content with project-wide instructions for AI editors. The same
file is mirrored into `.cursor/rules/`, `.claude/rules/`, `.codex/rules/` and
`.qoder/rules/` by `agentcs sync`.
